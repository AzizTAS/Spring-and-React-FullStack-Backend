# Production Configuration - Railway PostgreSQL
spring.datasource.url=jdbc:postgresql://crossover.proxy.rlwy.net:17466/railway
spring.datasource.username=postgres
spring.datasource.password=QvMjBtCxXFXJBiaBbnOynCebVLrjJfbp
spring.datasource.driver-class-name=org.postgresql.Driver

# Server Port - Use Railway's PORT env variable
server.port=${PORT:8080}

# HikariCP Connection Pool Settings for Railway
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.max-lifetime=120000
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.validation-timeout=5000

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.h2.console.enabled=false

# Email
hoaxify.email.host=${MAIL_HOST:smtp.gmail.com}
hoaxify.email.port=${MAIL_PORT:587}
hoaxify.email.username=${MAIL_USERNAME}
hoaxify.email.password=${MAIL_PASSWORD}
hoaxify.email.from=${MAIL_FROM}

# Client and Storage
hoaxify.client.host=https://spring-and-react-fullstack-frontend-production.up.railway.app
hoaxify.storage.root=uploads
hoaxify.token-type=opaque

# Pagination
spring.data.web.pageable.default-page-size=7

# Logging
logging.level.org.springframework.security=WARN

# Skip email verification in production (no SMTP server)
hoaxify.email.skip-verification=true
